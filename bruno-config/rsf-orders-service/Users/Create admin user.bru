meta {
  name: Create admin user
  type: http
  seq: 2
}

post {
  url: http://{{host}}:{{port}}/user
  body: json
  auth: inherit
}

body:json {
  {
    "name": "Yaash Jain",
    "email": "rohit@test.com",
    "kind": "admin"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
