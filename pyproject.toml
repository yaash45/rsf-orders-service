[project]
name = "rsf-orders-service"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pydantic>=2.12.3",
]

[project.scripts]
serve = "app.main:run"

[dependency-groups]
dev = [
    "ipython>=9.6.0",
]
